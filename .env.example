# Example environment for local development

# PostgreSQL connection string (required)
DATABASE_URL="postgresql://user:password@localhost:5432/agent_docs"

# OpenAI API key for embeddings (optional locally)
#OPENAI_API_KEY="sk-your-openai-key"

# Server settings
MCP_HOST=0.0.0.0
MCP_PORT=3001
RUST_LOG=info,doc_server=debug

# Tools configuration (mount tools.json in production)
#TOOLS_CONFIG_PATH="./tools.json"

# Discovery / loader settings
#CLAUDE_BINARY_PATH=claude
#LOADER_BIN=/app/loader
#INGEST_WORK_DIR=/tmp
